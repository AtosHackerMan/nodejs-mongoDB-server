//npm i nodemon -g --> This refreshes the terminal on change
//npm init --> initializes your project and creates a package.json file
//-D adds the install to the dev dependencies
//gitignore ignores the files in it when you make a push to github
//rm removes dependencies
//rm -D removes dev dependencies
// ^4.2.1 this allows minor updates and patches updates
// ~4.2.1 this only allows patches
// * this allows everything